

Required dependencies: Playwright
- Installation guide:
run the following commands in terminal:
npm install
npx playwright install

How to run tests Emin's test cases: 
Tests should be run individually, each test should follow the following structure:
In the terminal run for each test run:
npx playwright test ./tests/NAME_OF_THE_TEST_FILE
